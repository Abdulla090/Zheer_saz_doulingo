/**
 * Side-effect imports so expo-widgets bundles every widget at build time.
 * Import this once from the app root (_layout).
 */
import "./PhingoStreakWidget";
import "./PhingoDailyXpWidget";
import "./PhingoNextLessonWidget";
import "./PhingoProgressWidget";
