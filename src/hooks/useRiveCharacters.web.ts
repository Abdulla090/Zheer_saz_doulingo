// Web stub for useRiveCharacters since @rive-app/react-native does not support web.
// This prevents the app from crashing with "NativeModules.RiveRuntime is undefined".

const useRiveCharacters = () => {
  return { 
    duoRiveFile: null, 
    girlRiveFile: null, 
    manRiveFile: null 
  };
};

export default useRiveCharacters;
