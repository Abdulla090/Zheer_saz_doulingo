import GamesScreen from "@/screens/games/GamesScreen";
import React from "react";

/** GAMES tab — practice hub (role play, AI teacher, word games). */
export default function FeedRoute() {
  return <GamesScreen />;
}
